<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travel.web.admin.mapper.ItemFacilityMapper">

    <resultMap id="BaseResultMap" type="com.travel.model.pojo.ItemFacility">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="itemId" column="item_id" jdbcType="BIGINT"/>
            <result property="facilityId" column="facility_id" jdbcType="BIGINT"/>
            <result property="type" column="type" jdbcType="VARCHAR"/>
            <result property="sort" column="sort" jdbcType="INTEGER"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            <result property="isDeleted" column="is_deleted" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,item_id,facility_id,
        type,sort,create_time,
        update_time,is_deleted
    </sql>
    <select id="getFacilityIdListByTypeAndItemId" resultType="java.lang.Long">
        SELECT
            itf.facility_id
        FROM item_facility itf
                 INNER JOIN facility_info fi ON itf.facility_id = fi.id
        WHERE itf.item_id = #{id}
          AND itf.type = #{type}
          AND itf.is_deleted = 0
          AND fi.is_deleted = 0
          AND fi.type = #{facilityType}
    </select>
</mapper>
